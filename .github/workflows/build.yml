name: Compilar APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Descargar fuente D-DIN
        run: |
          mkdir -p fonts
          curl -sL -o ddin.zip https://raw.githubusercontent.com/amcchord/datto-d-din/master/D-DIN-ttf.zip \
            || curl -sL -o ddin.zip https://raw.githubusercontent.com/amcchord/datto-d-din/main/D-DIN-ttf.zip
          unzip -o ddin.zip -d ddin_extract
          cp "$(find ddin_extract -name 'D-DIN.ttf' | head -1)" fonts/D-DIN.ttf
          cp "$(find ddin_extract -name 'D-DIN-Bold.ttf' | head -1)" fonts/D-DIN-Bold.ttf
          ls -la fonts/

      - name: Firma fija (actualizaciones sin desinstalar)
        run: |
          mkdir -p ~/.android
          cp debug.keystore ~/.android/debug.keystore

      - name: Generar proyecto Android
        run: flutter create . --platforms android --project-name cuchillas

      - name: Dependencias
        run: flutter pub get

      - name: Compilar APK
        run: flutter build apk --release

      - name: Publicar APK
        uses: actions/upload-artifact@v4
        with:
          name: cuchillas-apk
          path: build/app/outputs/flutter-apk/app-release.apk
